apiVersion: v1
kind: Service
metadata:
  name: sync-orchestrator
  namespace: audiobookshelf
  labels:
    app: sync-orchestrator
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8080
  selector:
    app: sync-orchestrator
