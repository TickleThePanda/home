apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ingress
spec:
  entryPoints:
    - int-web
  routes:
    - kind: Rule
      match: Host(`internal.ticklethepanda.co.uk`)
      services:
        - name: internal-index-service
          namespace: internal-index
          port: 80
