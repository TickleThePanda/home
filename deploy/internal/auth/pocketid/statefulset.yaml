apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: pocket-id
  namespace: pocket-id
  labels:
    app: pocket-id
spec:
  serviceName: pocket-id
  replicas: 1
  selector:
    matchLabels:
      app: pocket-id
  template:
    metadata:
      labels:
        app: pocket-id
    spec:
      containers:
        - name: pocket-id
          image: ghcr.io/pocket-id/pocket-id:v2
          resources:
            requests:
              cpu: 10m
              memory: 48Mi
            limits:
              memory: 128Mi
          ports:
            - containerPort: 1411
              name: http
          env:
            - name: APP_URL
              value: "https://auth.internal.ticklethepanda.co.uk"
            - name: ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: pocket-id-secret
                  key: ENCRYPTION_KEY
            - name: UI_CONFIG_DISABLED
              value: "true"
            - name: LDAP_ENABLED
              value: "true"
            - name: LDAP_URL
              value: "ldap://lldap-service.lldap.svc.cluster.local:3890"
            - name: LDAP_BIND_DN
              value: "uid=admin,ou=people,dc=ticklethepanda,dc=co,dc=uk"
            - name: LDAP_BIND_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: pocket-id-secret
                  key: LDAP_BIND_PASSWORD
            - name: LDAP_BASE
              value: "dc=ticklethepanda,dc=co,dc=uk"
            - name: LDAP_USER_SEARCH_FILTER
              value: "(&(objectClass=person)(memberOf=cn=pocket_id_user,ou=groups,dc=ticklethepanda,dc=co,dc=uk))"
            - name: LDAP_USER_GROUP_SEARCH_FILTER
              value: "(objectClass=groupOfUniqueNames)"
            - name: LDAP_ATTRIBUTE_GROUP_MEMBER
              value: "uniqueMember"
            - name: LDAP_ATTRIBUTE_USER_UNIQUE_IDENTIFIER
              value: "uuid"
            - name: LDAP_ATTRIBUTE_GROUP_UNIQUE_IDENTIFIER
              value: "cn"
            - name: LDAP_ATTRIBUTE_GROUP_NAME
              value: "cn"
            - name: LDAP_ADMIN_GROUP_NAME
              value: "pocket_id_admin"
            - name: LDAP_ATTRIBUTE_USER_USERNAME
              value: "uid"
            - name: LDAP_ATTRIBUTE_USER_EMAIL
              value: "mail"
            - name: LDAP_ATTRIBUTE_USER_FIRST_NAME
              value: "firstname"
            - name: LDAP_ATTRIBUTE_USER_LAST_NAME
              value: "lastname"
            # Routed through deploy/internal/services/smtp-relay/ (an
            # in-cluster null relay forwarding to ProtonMail) rather than
            # ProtonMail directly -- no credential needed here any more, the
            # relay authenticates upstream itself. SMTP_TLS: none since this
            # leg never leaves the cluster; real TLS still happens on the
            # relay's own leg to ProtonMail.
            - name: SMTP_HOST
              value: "smtp-relay.smtp-relay.svc.cluster.local"
            - name: SMTP_PORT
              value: "587"
            - name: SMTP_TLS
              value: "none"
            - name: SMTP_FROM
              value: "noreply@ticklethepanda.co.uk"
          volumeMounts:
            - name: data
              mountPath: /app/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: [ "ReadWriteOnce" ]
        storageClassName: "lvm-data"
        resources:
          requests:
            storage: 128Mi
