apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ingress
spec:
  entryPoints:
    - int-web
  routes:
    - kind: Rule
      match: Host(`lldap.internal.ticklethepanda.co.uk`)
      services:
        - name: lldap-service
          namespace: lldap
          port: 80
