apiVersion: v1
kind: Service
metadata:
  name: geopulse-backend
  labels:
    app: geopulse-backend
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app: geopulse-backend
