apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kea

resources:
  - ./namespace.yaml
  - ./network-attachment.yaml
  - ./volume.yaml
  - ./deployment.yaml

configMapGenerator:
  - name: kea-config
    files:
      - dhcp4.json
