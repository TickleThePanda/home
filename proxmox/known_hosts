# SSH host key for proxmox-01 (192.168.1.3).
#
# Pinned here so CI verifies the host rather than running with host key
# checking disabled. CI reaches the host through the in-cluster cloudflared
# tunnel, so "trust on first use" would mean trusting whatever answers on the
# far side.
#
# Fingerprint: SHA256:2fO8ss+ZYTVZreSED9MFRGkne+z1R3AbM/W8kXcthBI
#
# If this legitimately changes (reinstall, sshd host key regenerated), update
# it with:
#   ssh-keyscan -t ed25519 192.168.1.3
# and verify the fingerprint from the host's console before committing.
192.168.1.3 ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIM5Krn4CNtytquwrxTw1wweS2mbpX5ctZmlbc7plyo+s
